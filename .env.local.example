# Copy to .env.local, then use docker compose --env-file .env.local up --build.
# Etherscan and an Ethereum RPC endpoint are needed to ingest real chain history.
ETHERSCAN_API_KEY=
WEB3_URL=
PIXELMAP_DB_PORT=15433
PIXELMAP_ASSET_PORT=3001
PIXELMAP_WEB_PORT=3000
