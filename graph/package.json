{
  "name": "pixelmap-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pixelmap",
    "create-local": "graph create --node http://localhost:8020/ pixelmap",
    "remove-local": "graph remove --node http://localhost:8020/ pixelmap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pixelmap"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}

