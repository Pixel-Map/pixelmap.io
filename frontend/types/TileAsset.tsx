export interface TileAsset {
  id?: number,
  image?: string,
  url?: string,
  price?: string,
  owner?: string,
  wrapped?: boolean,
  openseaPrice?: string,
  newPrice?: string,
  updating?: string,
  errorMessage? :string
}
